#
# This file will add the uuid to ciniki_events
# which is required for the sync to work.
#
ALTER TABLE ciniki_materiamedica_plants ADD COLUMN image_caption varchar(250) not null AFTER image_id;
ALTER TABLE ciniki_materiamedica_plants COMMENT='v1.02';
